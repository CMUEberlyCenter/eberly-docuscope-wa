# myScribe front end interface

## Development Usage

1. Clone this repo
1. `npm ci` to install dependencies.
1. `npm run dev` to start the development server.

## Notes:

* When testing with a LMS served locally via docker, it might be necessary to manually update the public keyset to http://host.docker.internal:8088/lti/keys
