# Pages for tools where the writing task id is embedded in the url.

These pages use the @id uri parameter to determin the writing task to use.
This useful when human readable short names for writing tasks are used but can still be used with database ids for private writing tasks.
